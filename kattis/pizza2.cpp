#include <bits/stdc++.h>
using namespace std;

int main() {
	int r, c;
	cin >> r >> c;
	float final = pow((r-c), 2) / pow(r, 2);
	cout << fixed << setprecision(7) << final = pow((r-c), 2) / pow(r, 2) << endl;
}