#include <bits/stdc++.h>
using namespace std;

vector<vector<int>> 

int main() {
	int n;
	ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
	while (cin >> n) {
		vector<string> names;
		for (int i = 0; i < n; i++) {
			int a;
			cin >> a;
			names.push_back(a);
		}
		sort(names.begin(), names.end());

	}
}