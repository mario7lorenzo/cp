#include <bits/stdc++.h>
using namespace std;

int main() {
	int testcase;
	cin >> testcase;
	for (int i = 0; i < testcase; i++) {
		string p, sentences;
		cin >> sentences;
		while (getline(cin >> p), p != "what does the fox say?") {
			string animal, goes, sound;
			cin >> animal >> goes >> sound;
			
		}
	}
}