#include <bits/stdc++.h>
using namespace std;

int main() {
	int r;
	cin >> r;
	double euclid = M_PI *r*r;
	double taxi = 2 * r * r;
	cout << fixed << setprecision(10) << euclid << endl;
	cout << fixed << setprecision(10) << taxi << endl;
}